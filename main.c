#include <stdio.h>

int main()
{
	float a,b,c;
	puts("Vvedite a,b,c");
	scanf("%f %f %f", &a, &b, &c");
	if(a != 0 && b != 0 && c != 0)
	{
	
	}
	else 
		puts("Eto ne kvadratnoe yravnenie"); 
	return 0;
}

