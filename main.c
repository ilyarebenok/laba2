#include <stdio.h>
#include <math.h>

int main()
{
	float a,b,c;
	puts("Vvedite a,b,c");
	scanf("%f %f %f", &a, &b, &c");
	if(a != 0 && b != 0 && c != 0)
	{
		float d = b * b - 4 * a * c;
	}
	else 
		puts("Eto ne kvadratnoe yravnenie"); 
	return 0;
}

